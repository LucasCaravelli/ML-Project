You are generating a single high-quality SYNTHESIS question for a retrieval-augmented generation benchmark.

Later, smaller models will try to answer your question by retrieving chunks from the corpus, so the question must be answerable ONLY from the provided source text — do not rely on outside knowledge.

Do NOT write the answer. Only produce the question. The answer will be generated separately in a follow-up step.

Question type: synthesis
- A synthesis question requires integrating information spread across the PRIMARY and NEIGHBOR chunks into a single concise claim.
- Downstream answer generation will cap the answer at ≤ 25 words (a tight integrated phrase, not a paragraph), so the question must be phrased so a correct answer fits within that cap. This cap is a downstream constraint, not the optimization target — the priority is meaningful integration, not brevity.

PRIMARY chunk (the question MUST be anchored here):
<<<
{primary_chunk}
>>>

NEIGHBOR chunks (additional context required for synthesis):
<<<
{neighbor_chunks}
>>>

Hard requirements:
1. The answer must integrate at least two supporting statements into one concise claim, including at least one from the PRIMARY chunk.
2. Prefer comparison, trade-off, role, implication, contrast, or condition questions.
3. "Why", "how", "under what condition", "what trade-off", and "how does X differ from Y" forms are explicitly allowed if the answer can still be concise.
4. Reject generic summary questions ("What does this section describe?", "Summarize the discussion of X").
5. Reject questions whose answer is just a copied phrase from one sentence.
6. Skip if the question would still be answerable after removing the NEIGHBOR chunks.
7. Do not ask meta-questions about the text itself. Ask about the content.
8. Do not leak the answer phrasing inside the question.
9. Avoid ambiguous references such as unclear pronouns, "the method," "the model," or "this result" unless uniquely identifiable from the question alone.
10. Do not invent names, numbers, entities, relationships, or cross-chunk links not supported by the text.
11. Use the terminology from the text for factual accuracy and grounding, but do not force maximal lexical overlap if that would make the question overly extractive or trivial.
12. Keep the question under 35 words.
13. The question is only valid if a correct answer fits within ≤ 25 words as a tight integrated phrase. If no such question is possible for this chunk, skip.

Internal validity check (perform silently before producing the final JSON):
1. The answer is fully supported by the text.
2. The answer requires integrating at least two distinct supporting statements (one from PRIMARY, at least one from NEIGHBOR).
3. The question is not answerable from one sentence alone.
4. The question is not a generic summary or open-ended opinion request.
5. The question is not a simple lookup disguised as synthesis.
If any check fails, skip.

Skip rule:
If the provided text does not support a clearly valid synthesis question, skip. It is better to skip than to produce a borderline or weak question. Use:
{{"skip": true, "reason": "<short reason>"}}

Output format — respond with a single JSON object on one line, no prose, no code fences:
{{"question": "<the question>"}}

Or, if the chunk is unsuitable:
{{"skip": true, "reason": "<short reason>"}}
